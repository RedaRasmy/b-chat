import { Router } from "express"

const router = Router()

// router.post("/dm", addDM)
// router.post("/group",addGroup)
// router.get("/", getChannels)
// router.get("/:id/messages", getChannelMessages)

export const channelsRouter = router
